# OpenRing Backend

Express + Apollo + type-graphql + TypeORM + PostgreSQL.

```bash
npm install
npm run dev      # http://localhost:4000/graphql
npm test
```

See root `README.md` for Docker and full assessment docs.
